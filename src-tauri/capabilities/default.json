{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ImageCreator デフォルト権限セット。各 Phase で必要な権限を追加する。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:default",
    "fs:default"
  ]
}
